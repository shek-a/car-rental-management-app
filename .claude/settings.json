{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.ts) npx eslint --fix \"$f\";; esac; } 2>/dev/null || true",
            "statusMessage": "Linting TypeScript with ESLint"
          }
        ]
      }
    ]
  }
}
